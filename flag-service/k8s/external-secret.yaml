apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: flag-service-secrets
  namespace: flag-service
spec:
  refreshInterval: 30s
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: flag-service-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: eks/flag-service
